# ShellSpec configuration for harm-cli

## Output format
--format documentation
--color

## Shell to use (bash 5+ required for associative arrays)
## Note: Shell path can be overridden via SHELLSPEC_SHELL env var or -s flag
## Default: use system bash (ensure 5+ is installed)
--shell bash

## Coverage (uncomment to enable)
# --kcov

## Fail fast (uncomment to stop on first failure)
# --fail-fast

## Random execution order (uncomment for randomization)
# --random

## Warnings
# Warnings are informational only (stderr output expected in complex tests)
# Don't treat warnings as failures - DEBUG logging to stderr is expected
--no-warning-as-failure

## Execution
# Use sequential execution to avoid hanging issues
# --jobs 4
--jobs 1
